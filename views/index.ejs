<!DOCTYPE html>
<html>

<head>
  <title>sweet recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Cake & Bake 🍰</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="doughnuts" name="choice">🍩</button>
    <button type="submit" value="icecream" name="choice">🍨</button>
    <button type="submit" value="cake" name="choice">🎂</button>
  </form>
  <div id="recipeContainer" class="hidden">
    <div id="recipeContainer" class="hidden">
      <%if (locals.recipe) {%>
  
        <h2 id="recipeTitle">
          <%= recipe.name %>
        </h2>
  
        <h3>Ingredients:</h3>
        <ul id="ingredientsList">
          <li>
            <%= recipe.ingredients.method.name %>, <%= recipe.ingredients.method.preparation %>
          </li>
          <li>
            <%= recipe.ingredients.Type.name %>
  
          </li>
          <% recipe.ingredients.toppings.forEach(topping=> { %>
            <li>
              <%= topping.quantity %> of <%= topping.name %>
            </li>
            <% }) %>
        </ul>
  
        <%} else {%>
          <h2>Pick your favourite recipe from Cake & Bake 😉</h2>
          <%}%>
    </div>
   


  </div>
</body>

</html>